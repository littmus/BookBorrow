{% extends 'base.djhtml' %}

{% block contents %}

<section id="book">
    <div class="span12">
        <h2>{{ book.library.name }}</h2>
        <h1>{{ book.title }}</h1>
    </div>
</section>

{% endblock %}